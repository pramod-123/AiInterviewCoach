# When the Git repo root is the monorepo (`AiInterviewCoach/`), see root `.gitignore`.
# This file remains for standalone `server/` checkouts or tooling that only reads this folder.

node_modules/
dist/
coverage/
data/
data.backup-*/
.env
.app-runtime-config.json
*.log
.venv-whisperx/

media/*
!media/.gitkeep
